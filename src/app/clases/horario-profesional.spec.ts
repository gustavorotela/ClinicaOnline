import { HorarioProfesional } from './horario-profesional';

describe('HorarioProfesional', () => {
  it('should create an instance', () => {
    expect(new HorarioProfesional()).toBeTruthy();
  });
});
